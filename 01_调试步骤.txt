
cd /d D:\Study\codexTest\CodexLOFarb

python LOF011_generate_basic_data.py
 011 ：去 data\woodyAPI\ 目录下， 看看是否生成了今天最新的API .json 和 .csv， 打开.csv文件检查是否正常，务必看懂。这里面是“基准日”的数据。


打开 data\ 目录下 GLD_USO_basic_data.csv  务必看懂

python LOF012_generate_lof_data.py
打开 基金历史文件表格 data\LOF_160719_history.csv（或其他 LOF 表格）。
检查 012 是否正确地触发了 013 去 Woody 网页爬取了 “估值日”GLD 和 ^GLD-EU 的数据，并填回了 basic 表。
检查 012 是否去东财和新浪补齐了 T-1 （估值日）日的“LOF净值”和“LOF收盘价”。
最核心的一步：检查表格右侧的 ETF静态估值 列，看程序算出来的 T-1 日估值，是否和你刚才在 Excel 里手工计算的结果 100% 吻合！


第三步：手动启动后台服务 (可选，仅为实时数据准备)
如果你想把整套系统跑起来看网页，你需要开启后台服务。因为后台服务会持续运行阻塞窗口，建议你新开两个 CMD 窗口（同样先 cd /d D:\Study\codexTest\CodexLOFarb），分别运行：

在窗口 A 运行（启动维护面板服务 5002端口）：

cmd
python LOF01_admin_launcher.py
在窗口 B 运行（启动行情引擎 5000端口）：

cmd
python LOF02_fetch_trade_data.py


四步：手动生成监控 HTML 页面
回到你最初的那个 CMD 窗口，运行：

cmd
python LOF03_generate_monitor_html


运行完毕后，你可以直接双击目录下的 lof_monitor.html，或者在浏览器打开 http://localhost:5000/，看看在刚才完美的数据基石上，页面渲染出来的静态估值和 T-1 日溢价是否一切正常。