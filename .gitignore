# ============================================================
# arbTest 源码仓库（项目根 = src/，物理隔离）
# D:\Study\arbTest 下所有 "." 开头的 AI/工具目录
# (.workbuddy/.opencode/.codegraph/.omo/.open-mem 等) 天然在仓库外，不上传。
# 本文件只管 src/ 内部的敏感/非源码内容。
# ============================================================

# --- 实盘数据库（不传）---
# 活库/分享库已整体移出仓库根到 D:\Study\arbTest\database\（物理隔离，src 内零 database）
# 此处 *.db 等规则仅作纵深防御：万一 src 内误落任何 db 也绝不传。
*.db
# SQLite 运行时 sidecar（实盘库孪生体，绝不传）
*.db-shm
*.db-wal

# --- 数据库 / 仓库 备份（不传）---
**/*.bak*
**/*.bundle

# --- 密钥 / 私密配置 ---
account_private.py
arbcore/scripts/
**/private/
**/Lazy*
**/*lazy*

# --- 交易策略 / 私密模块 ---
**/auto_trade/
# 根目录项目指引（含内部口径，不传）
agents.md
# 本地代理/VPN 启动脚本（仅本机用，不传）
Proxy.bat
# 交易执行层（QMT/IB 下单管理、策略信号、前端下单逻辑，不传）
arbcore/traders/
**/signal_detector.py
**/useOrderLogic.js
# 「我的交易」菜单页 + 其 API 客户端（不传）
**/AutoTrade.vue
**/AutoTrade*
**/autoTrade*

# --- 日志（不传）---
*.log

# --- 续传 session / 内部运行态（不传）---
.omo/

# --- 项目内数据目录（交易记录/同步/验证/woodyAPI，含账户与内部数据，不传）---
ArbDashboard/data/

# --- 临时 / 运行时 json（不传）---
**/_ps_tmp.json
**/rt_cache.json
**/rt_freeze.json

# --- AI 临时调试脚本（不传）---
_ai_backup/
scratch/

# --- 构建产物 & 依赖 ---
__pycache__/
*.pyc
node_modules/
.venv/
venv/

# --- 项目非源码目录（含内部文档，不传）---
deploy/
docs/
docs_unfinished/
logs/

# --- 前端构建缓存 ---
dist/
.vite/
